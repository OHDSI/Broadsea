version: '2'

services:

  broadsea-methods-library:
    image: ohdsi/broadsea-methods-library
    ports:
      - "8787:8787"
      - "6311:6311"
    volumes:
      - ./home/rstudio:/home/rstudio

  broadsea-webtools:
    image: ohdsi/broadsea-webtools
    ports:
      - "8080:8080"
    volumes:
     - .:/tmp/drivers/:ro
     - .:/tmp/achilles-data-reports/:ro
    environment:
     - WEBAPI_URL=http://192.168.99.100:8080
     - env=webapi-mssql
     - datasource_driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
     - datasource_url=jdbc:sqlserver://192.168.99.100;databasename=secret-database-name
     - datasource_username=secret-user-name
     - datasource_password=secret-user-password
     - spring_jpa_properties_hibernate_default_schema=ohdsi
     - flyway_datasource_driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
     - flyway_datasource_url=jdbc:sqlserver://192.168.99.100;databasename=secret-database-name
     - flyway_schemas=ohdsi
     - flyway_placeholders_ohdsiSchema=ohdsi
     - flyway_datasource_username=secret-user-name
     - flyway_datasource_password=secret-user-password
     - flyway_locations=classpath:db/migration/sqlserver
